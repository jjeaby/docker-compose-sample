FROM debian:jessie-slim

ENV NAME_ENV=b
ARG NAME_ARG=b

RUN echo "envirment = ${NAME_ENV:-b}" \
    && echo "argument = ${NAME_ARG:-b}"

CMD echo "envirment = ${NAME_ENV:-b}" \
    && echo "argument = ${NAME_ARG:-b}"